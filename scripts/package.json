{
  "name": "follow-builders-scripts",
  "version": "1.0.0",
  "description": "Scripts for Follow Builders skill — feed generation, digest preparation, delivery",
  "type": "module",
  "scripts": {
    "generate-feed": "node generate-feed.js",
    "prepare-digest": "node prepare-digest.js",
    "build-structured-history": "node build-structured-history.js",
    "fetch-external-sources": "node fetch-external-sources.js",
    "sync-site-data": "node sync-site-data.js",
    "test": "node --test tests/*.test.js",
    "validate-feeds": "node validate-feeds.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}
